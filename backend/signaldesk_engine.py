"""\nsignaldesk_engine.py\nMain orchestrator. Imports ONLY from: base_indicators, data_fetchers,\noptions_analyzer, constants, cache. No circular deps.\n\nPerformance: Uses ThreadPoolExecutor to run 7 external data calls\nin parallel (~3s instead of ~15s sequential).\n"""\n\nimport os\nimport sys\n\nsys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))\n\nimport threading\nimport time as _time\n\nfrom signal_engine.contributors import (\n    OptionsContributor, BreadthContributor, VolatilityContributor,\n    MacroContributor, InstitutionalContributor, RegimeContributor, TechnicalContributor\n)\n\n_CONTRIBUTORS = [\n    OptionsContributor(),\n    BreadthContributor(),\n    VolatilityContributor(),\n    MacroContributor(),\n    InstitutionalContributor(),\n    RegimeContributor(),\n    TechnicalContributor(),\n]\n\nfrom datetime import datetime\n\nimport numpy as np\nimport pandas as pd\n\nimport cache as C\nimport yfinance as yf\nfrom base_indicators import (\n    build_chart_records,\n    calc_adx,\n    calc_atr,\n    calc_bb,\n    calc_ema,\n    calc_entry_exit,\n    calc_macd,\n    calc_rsi,\n    calc_sr_levels,\n    calc_stoch_rsi,\n    calc_tech_score_adx,\n    calc_vol_ratio,\n    candle_pattern,\n    candle_pattern_series,\n    safe,\n)\nfrom constants import SYM_MAP\nfrom data_fetchers import (\n    get_fii_dii,\n    get_global_markets,\n    get_india_vix,\n    get_multi_tf,\n    get_news_sentiment,\n)\n\n_adv_dec_cache = {"data": None, "ts": 0}\n_adv_dec_lock = threading.Lock()\n_CACHE_TTL = 900  # 15 min\n\n\ndef get_advance_decline_cached(symbol):\n    """FIX #8 — cached so it doesn't slow every request."""\n    now = _time.time()\n    with _adv_dec_lock:\n        if _adv_dec_cache["data"] and (now - _adv_dec_cache["ts"]) < _CACHE_TTL:\n            return _adv_dec_cache["data"]\n\n    from constants import NIFTY_50\n\n    advances = declines = unchanged = 0\n    # Batch download instead of 20 individual Ticker calls\n    try:\n        syms_str = " ".join(NIFTY_50[:20])\n        df = yf.download(\n            syms_str,\n            period="5d",\n            interval="1d",\n            progress=False,\n            auto_adjust=True,\n            group_by="ticker",\n            timeout=15,\n        )\n        if not df.empty:\n            df = df.loc[~df.index.duplicated(keep="last")]\n\n        for sym in NIFTY_50[:20]:\n            try:\n                sub = pd.DataFrame()\n                if isinstance(df.columns, pd.MultiIndex):\n                    if sym in df.columns.get_level_values(0):\n                        sub = df[sym].dropna(subset=["Close"])\n                elif sym in df.columns:\n                    sub = df[[sym]].dropna()\n\n                if len(sub) >= 2:\n                    # Flatten columns if MultiIndex\n                    if isinstance(sub.columns, pd.MultiIndex):\n                        for i in range(sub.columns.nlevels):\n                            if "Close" in sub.columns.get_level_values(i):\n                                sub.columns = sub.columns.get_level_values(i)\n                                break\n\n                    last = float(sub["Close"].iloc[-1])\n                    prev = float(sub["Close"].iloc[-2])\n                    chg = (last - prev) / prev * 100\n                    if chg > 0.1:\n                        advances += 1\n                    elif chg < -0.1:\n                        declines += 1\n                    else:\n                        unchanged += 1\n                else:\n                    unchanged += 1\n            except (TypeError, KeyError, IndexError, ValueError, AttributeError):\n                unchanged += 1\n    except Exception:\n        # Fallback: return neutral\n        advances = 10\n        declines = 10\n        unchanged = 0\n\n    total = max(1, advances + declines + unchanged)\n    result = {\n        "advances": advances,\n        "declines": declines,\n        "unchanged": unchanged,\n        "total": total,\n        "mood": (\n            "BULLISH"\n            if advances > declines * 1.5\n            else "BEARISH"\n            if declines > advances * 1.5\n            else "NEUTRAL"\n        ),\n        "advance_pct": round(advances / total * 100, 1),\n        "note": "Sample of 20 Nifty stocks",\n    }\n    with _adv_dec_lock:\n        _adv_dec_cache["data"] = result\n        _adv_dec_cache["ts"] = _time.time()\n    return result\n\n\ndef map_confidence(score):\n    """\n    Converts a raw signal score (-10 to +10) into a labeled confidence object.\n    Used for UI badges and color-coding.\n    """\n    abs_score = abs(score)\n    if abs_score >= 7:\n        label = "HIGH"\n    elif abs_score >= 4:\n        label = "MEDIUM"\n    else:\n        label = "LOW"\n    return {"label": label, "score": score}\n\n\ndef score_signal(tech, mtf, vix, global_bias, fii=0, oi=0, news=0, quant_conf=0):\n    """FIX #34 — quant confidence now feeds into signal score."""\n    s = {\n        "TECH": max(-6, min(6, tech)),\n        "OI": max(-4, min(4, oi)),\n        "NEWS": max(-2, min(2, news)),\n        "MTF": -2 if "BEAR" in (mtf or "") else 2 if "BULL" in (mtf or "") else 0,\n        "GLOBAL": 2\n        if global_bias == "BULLISH"\n        else -2\n        if global_bias == "BEARISH"\n        else 0,\n        "VIX": -2 if (vix or 16) > 20 else -1 if (vix or 16) > 16 else 1,\n        "FII": 2 if fii > 500 else -2 if fii < -500 else 0,\n        "QUANT": (\n            2\n            if quant_conf >= 0.75\n            else 1\n            if quant_conf >= 0.4\n            # FIX #34\n            else -1\n            if quant_conf <= -0.4\n            else -2\n            if quant_conf <= -0.75\n            else 0\n        ),\n    }\n    total = sum(s.values())\n    conf_obj = map_confidence(total)\n\n    if total >= 7:\n        sig, urg = "BUY_CALL", "NOW"\n    elif total >= 4:\n        sig, urg = "BUY_CALL", "WAIT_CONFIRM"\n    elif total <= -7:\n        sig, urg = "BUY_PUT", "NOW"\n    elif total <= -4:\n        sig, urg = "BUY_PUT", "WAIT_CONFIRM"\n    else:\n        sig, urg = "NO_TRADE", "WAIT"\n\n    return {\n        "signal": sig,\n        "urgency": urg,\n        "confidence": conf_obj,\n        "score": total,\n        "breakdown": s,\n    }\n\n\ndef get_economic_calendar():\n    """get_economic_calendar function.\n\n    Args:\n        None\n        \n    Returns:\n        Any: The computed result.\n        \n    Raises:\n        Exception: If the operation fails.\n        \n    Examples:\n        >>> result = get_economic_calendar()\n    """\n    today = datetime.now()\n    is_exp = today.weekday() <= 3\n    days_thu = max(0, 3 - today.weekday())\n    events = [\n        {"date": "Every Thursday", "event": "NSE weekly F&O expiry", "impact": "HIGH"},\n        {\n            "date": "Last Thursday/month",\n            "event": "NSE monthly expiry",\n            "impact": "HIGH",\n        },\n        {"date": "Quarterly", "event": "RBI monetary policy", "impact": "HIGH"},\n        {"date": "Quarterly", "event": "Corporate earnings", "impact": "MEDIUM"},\n        {"date": "Annual Feb 1", "event": "Union Budget", "impact": "EXTREME"},\n    ]\n    upcoming = []\n    if days_thu <= 3:\n        upcoming.append(\n            {"event": "Weekly F&O expiry", "days_away": days_thu, "impact": "HIGH"}\n        )\n    return {\n        "events": events,\n        "upcoming": upcoming,\n        "note": "Expiry week — elevated volatility" if is_exp else "",\n    }\n\n\ndef _compute_market_context(df, price):\n    closes = df["Close"]\n    rsi_v = safe(calc_rsi(closes).iloc[-1])\n    macd_l, macd_s, macd_h = calc_macd(closes)\n    bb_u, bb_m, bb_l = calc_bb(closes)\n    stoch_k = safe(calc_stoch_rsi(closes).iloc[-1])\n    ema9 = safe(calc_ema(closes, 9).iloc[-1])\n    ema20 = safe(calc_ema(closes, 20).iloc[-1])\n    ema50 = safe(calc_ema(closes, 50).iloc[-1])\n    atr_v = safe(calc_atr(df).iloc[-1])\n    adx_s, pos_di, neg_di = calc_adx(df)\n    adx_v = safe(adx_s.iloc[-1])\n    candle = candle_pattern(df)\n    prev = safe(closes.iloc[-2])\n    chg = round((price - prev) / prev * 100, 2) if (price and prev) else 0\n    vr = calc_vol_ratio(df)\n    vol_ma20 = float(df["Volume"].rolling(20).mean().iloc[-1]) if not df.empty else 0\n    vol_current = float(df["Volume"].iloc[-1]) if not df.empty else 0\n    vol_confirms = bool(vol_current > vol_ma20)\n    levels = calc_sr_levels(df)\n\n    ts = 0\n    if rsi_v:\n        ts += (\n            3\n            if rsi_v < 30\n            else 2\n            if rsi_v < 45\n            else -1\n            if rsi_v > 65\n            else -3\n            if rsi_v > 75\n            else 0\n        )\n    mn = safe(macd_l.iloc[-1])\n    mp = safe(macd_l.iloc[-2])\n    sn = safe(macd_s.iloc[-1])\n    sp = safe(macd_s.iloc[-2])\n    if mn and sn:\n        if (mp or 0) < (sp or 0) and mn > sn:\n            ts += 3\n        elif (mp or 0) > (sp or 0) and mn < sn:\n            ts -= 3\n        elif mn > sn:\n            ts += 1\n        else:\n            ts -= 1\n    if price and ema20:\n        ts += 1 if price > ema20 else -1\n    if vr and vr > 1.5:\n        ts += 1 if chg > 0 else -1\n    ts = calc_tech_score_adx(ts, adx_v)\n\n    stoch_d = safe(\n        calc_ema(\n            pd.Series(\n                df["Stoch_K"] if "Stoch_K" in df.columns else calc_stoch_rsi(closes)\n            ),\n            3,\n        ).iloc[-1]\n    )\n\n    c5_p = safe(closes.iloc[-6]) if len(closes) > 6 else None\n    c15_p = safe(closes.iloc[-16]) if len(closes) > 16 else None\n    c30_p = safe(closes.iloc[-31]) if len(closes) > 31 else None\n    c5 = round((price - c5_p) / c5_p * 100, 3) if (price and c5_p) else 0\n    c15 = round((price - c15_p) / c15_p * 100, 3) if (price and c15_p) else 0\n    c30 = round((price - c30_p) / c30_p * 100, 3) if (price and c30_p) else 0\n\n    candle_momentum = {\n        "score": 1 if chg > 0 else -1 if chg < 0 else 0,\n        "bias": "BULLISH" if chg > 0 else "BEARISH" if chg < 0 else "NEUTRAL",\n        "c5": c5,\n        "c15": c15,\n        "c30": c30,\n        "movement": "UP"\n        if (c5 or 0) > 0.3\n        else "DOWN"\n        if (c5 or 0) < -0.3\n        else "SIDEWAYS",\n    }\n\n    df["RSI"] = calc_rsi(closes)\n    df["MACD"] = macd_l\n    df["MACD_Signal"] = macd_s\n    df["MACD_Hist"] = macd_h\n    df["BB_Upper"] = bb_u\n    df["BB_Mid"] = bb_m\n    df["BB_Lower"] = bb_l\n    df["EMA_9"] = calc_ema(closes, 9)\n    df["EMA_20"] = calc_ema(closes, 20)\n    df["EMA_50"] = calc_ema(closes, 50)\n    df["Stoch_K"] = calc_stoch_rsi(closes)\n    df["ADX"] = adx_s\n    df["+DI"] = pos_di\n    df["-DI"] = neg_di\n    df["Pattern"] = candle_pattern_series(df)\n\n    CHART_COLS = [\n        "Open",\n        "High",\n        "Low",\n        "Close",\n        "Volume",\n        "RSI",\n        "MACD",\n        "MACD_Signal",\n        "MACD_Hist",\n        "BB_Upper",\n        "BB_Mid",\n        "BB_Lower",\n        "EMA_9",\n        "EMA_20",\n        "EMA_50",\n        "Stoch_K",\n        "ADX",\n        "+DI",\n        "-DI",\n        "Pattern",\n    ]\n    chart = build_chart_records(df, CHART_COLS, n=120)\n\n    sup_str = f"₹{levels['support'][0]['level']:,.0f}" if levels["support"] else "—"\n    res_str = (\n        f"₹{levels['resistance'][0]['level']:,.0f}" if levels["resistance"] else "—"\n    )\n\n    market_overview = {\n        "price": price,\n        "trend": "DOWNTREND" if ts < 0 else "UPTREND" if ts > 0 else "SIDEWAYS",\n        "candle": candle,\n        "volume": {\n            "current": int(vol_current) if not np.isnan(vol_current) else 0,\n            "avg_20": int(vol_ma20) if not np.isnan(vol_ma20) else 0,\n            "ratio": vr,\n            "confirms": vol_confirms,\n            "note": "High Volume" if vol_confirms and (vr or 0) > 1.5 else "Normal",\n        },\n        "ema": {"9": ema9, "20": ema20, "50": ema50},\n        "rsi": rsi_v,\n        "stoch_rsi": stoch_k,\n        "macd_histogram": safe(macd_h.iloc[-1]),\n        "macd_line": safe(macd_l.iloc[-1]),\n        "signal_line": safe(macd_s.iloc[-1]),\n        "macd": safe(macd_l.iloc[-1]),\n        "macd_signal": safe(macd_s.iloc[-1]),\n        "bb_upper": safe(bb_u.iloc[-1]),\n        "bb_mid": safe(bb_m.iloc[-1]),\n        "bb_lower": safe(bb_l.iloc[-1]),\n        "stoch_k": stoch_k,\n        "stoch_d": stoch_d,\n        "atr": atr_v,\n        "vol_ratio": vr,\n        "volatility_pct": round(abs(chg), 2),\n        "adx": adx_v,\n        "pos_di": safe(pos_di.iloc[-1]),\n        "neg_di": safe(neg_di.iloc[-1]),\n        "support": sup_str,\n        "resistance": res_str,\n    }\n\n    return {\n        "ts": ts,\n        "chg": chg,\n        "levels": levels,\n        "atr_v": atr_v,\n        "candle": candle,\n        "rsi_v": rsi_v,\n        "mn": mn,\n        "sn": sn,\n        "vol_confirms": vol_confirms,\n        "stoch_k": stoch_k,\n        "stoch_d": stoch_d,\n        "sup_str": sup_str,\n        "res_str": res_str,\n        "chart": chart,\n        "market_overview": market_overview,\n        "candle_momentum": candle_momentum,\n    }\n\n\ndef _compute_options_sentiment(opts_data):\n    return {"oi_score": opts_data.get("oi_score", 0), "atm_iv": opts_data.get("atm_iv")}\n\n\ndef _compute_breadth_metrics(news_data):\n    return news_data.get("score", 0)\n\n\ndef _compute_regime_signal(\n    ts,\n    mtf_data,\n    vix_data,\n    global_data,\n    fii_data,\n    oi_score,\n    news_score,\n    quant_conf,\n    quant_data,\n):\n    fii_net = fii_data.get("fii_net", 0)\n    sig_data = score_signal(\n        ts,\n        mtf_data.get("alignment"),\n        vix_data.get("vix"),\n        global_data.get("bias"),\n        fii_net,\n        oi_score,\n        news_score,\n        quant_conf,\n    )\n    is_long_initial = ts >= 0\n    fii_aligned = bool(\n        (is_long_initial and (fii_net or 0) > 0)\n        or (not is_long_initial and (fii_net or 0) < 0)\n    )\n    final_score = sig_data["score"]\n    if fii_aligned:\n        final_score += 1 if is_long_initial else -1\n\n    conf_obj = map_confidence(final_score)\n    if final_score >= 7:\n        signal, urgency = "BUY_CALL", "NOW"\n    elif final_score >= 4:\n        signal, urgency = "BUY_CALL", "WAIT_CONFIRM"\n    elif final_score <= -7:\n        signal, urgency = "BUY_PUT", "NOW"\n    elif final_score <= -4:\n        signal, urgency = "BUY_PUT", "WAIT_CONFIRM"\n    else:\n        signal, urgency = "NO_TRADE", "WAIT"\n\n    sig_data.update(\n        {\n            "signal": signal,\n            "urgency": urgency,\n            "confidence": conf_obj,\n            "score": final_score,\n        }\n    )\n    is_long = final_score >= 0\n    mtf_data["aligned"] = (\n        "BULL" in mtf_data.get("alignment", "")\n        if is_long\n        else "BEAR" in mtf_data.get("alignment", "")\n    )\n    return sig_data, is_long, fii_aligned\n\n\ndef _compute_trade_levels(price, levels, atr_v, signal):\n    ds = (\n        round(abs(price - levels["support"][0]["level"]) / price * 100, 2)\n        if levels["support"]\n        else None\n    )\n    dr = (\n        round(abs(levels["resistance"][0]["level"] - price) / price * 100, 2)\n        if levels["resistance"]\n        else None\n    )\n    ns = ds is not None and ds < 0.5\n    nr = dr is not None and dr < 0.5\n    zone = "NEAR SUPPORT" if ns else "NEAR RESISTANCE" if nr else "MID RANGE"\n\n    entry_exit = calc_entry_exit(price, atr_v, signal)\n\n    sr_zone = {\n        "support_str": f"₹{levels['support'][0]['level']:,.0f}"\n        if levels["support"]\n        else "—",\n        "resistance_str": f"₹{levels['resistance'][0]['level']:,.0f}"\n        if levels["resistance"]\n        else "—",\n        "dist_to_support": ds,\n        "dist_to_resistance": dr,\n        "zone": zone,\n        "near_support": ns,\n        "near_resistance": nr,\n    }\n    return entry_exit, sr_zone, ns, nr\n\n\ndef _compute_risk_metrics(ts, vix_data, levels, price, atr_v):\n    monday_today = datetime.now().weekday()\n    days_thu = (3 - monday_today) % 7\n    rules = [\n        "Never trade in first 30 min (9:15–9:45 IST)",\n        f"Trend: {'DOWNTREND' if ts < 0 else 'UPTREND'} — confirm direction after 9:45",\n        f"PUT only if price breaks BELOW ₹{round((levels['support'][0]['level'] if levels['support'] else price - 50), 0)}",\n        f"CALL only if price breaks ABOVE ₹{round((levels['resistance'][0]['level'] if levels['resistance'] else price + 50), 0)}",\n    ]\n    if (vix_data.get("vix") or 0) > 20:\n        rules.append("VIX elevated — reduce size or use spreads")\n\n    monday = {\n        "trend": "DOWNTREND" if ts < 0 else "UPTREND",\n        "put_trigger": round(\n            (\n                levels["support"][0]["level"]\n                - (atr_v if atr_v is not None else 0.0) * 0.5\n            )\n            if levels["support"]\n            else price - 50,\n            0,\n        ),\n        "call_trigger": round(\n            (\n                levels["resistance"][0]["level"]\n                + (atr_v if atr_v is not None else 0.0) * 0.5\n            )\n            if levels["resistance"]\n            else price + 50,\n            0,\n        ),\n        "support": round(levels["support"][0]["level"], 0)\n        if levels["support"]\n        else None,\n        "resistance": round(levels["resistance"][0]["level"], 0)\n        if levels["resistance"]\n        else None,\n        "rules": [r for r in rules if r],\n    }\n    return monday\n\n\ndef _compute_summary(\n    is_long,\n    ns,\n    nr,\n    sup_str,\n    res_str,\n    vix_data,\n    rsi_v,\n    mn,\n    sn,\n    vol_confirms,\n    global_data,\n    fii_aligned,\n    sig_data,\n    entry_exit,\n):\n    reasons = []\n    if ns:\n        reasons.append(\n            f"Price is currently testing significant support at {sup_str}. Historical data suggests a high probability of a bounce from this zone."\n        )\n    elif nr:\n        reasons.append(\n            f"Price is encountering heavy resistance near {res_str}. A failure to break above could lead to a short-term pullback."\n        )\n\n    vix_v = vix_data.get("vix")\n    if vix_v and vix_v > 19:\n        reasons.append(\n            f"Elevated India VIX ({vix_v}) signals high market anxiety. Expect erratic price action and prioritize capital protection."\n        )\n    elif vix_v and vix_v < 13:\n        reasons.append(\n            "Low VIX environment suggests market complacency; ideal for steady trend-following strategies."\n        )\n\n    if is_long:\n        if rsi_v and rsi_v < 40:\n            reasons.append(\n                "RSI is in the accumulation zone, indicating a potential 'buy-the-dip' opportunity as the asset is oversold."\n            )\n        elif rsi_v and rsi_v > 60:\n            reasons.append(\n                "Strong bullish momentum is confirmed by RSI holding above 60, suggesting the trend has more room to run."\n            )\n        if (mn or 0) > (sn or 0):\n            reasons.append(\n                "A bullish MACD crossover has been detected, signaling an acceleration in upward price momentum."\n            )\n        if vol_confirms:\n            reasons.append(\n                "Rising volume validates the current price move, suggesting strong participation from larger market players."\n            )\n        if global_data.get("bias") == "BULLISH":\n            reasons.append(\n                "Positive global market sentiment (S&P 500/Nasdaq) is providing a favorable tailwind for Indian indices."\n            )\n        if fii_aligned:\n            reasons.append(\n                "FII net flows are positive and aligned with the current price direction, providing institutional validation."\n            )\n    else:\n        if rsi_v and rsi_v > 60:\n            reasons.append(\n                "RSI is showing overbought conditions, suggesting a high probability of mean reversion to the downside."\n            )\n        elif rsi_v and rsi_v < 40:\n            reasons.append(\n                "Bearish momentum is intensifying as RSI stays below 40, indicating sellers are firmly in control."\n            )\n        if (mn or 0) < (sn or 0):\n            reasons.append(\n                "A bearish MACD crossover suggests continued downside pressure in the near term."\n            )\n        if vol_confirms:\n            reasons.append(\n                "Increased selling volume on price drops indicates aggressive distribution by institutional desks."\n            )\n        if global_data.get("bias") == "BEARISH":\n            reasons.append(\n                "Global bearish sentiment is acting as a significant drag, increasing the risk of further local market corrections."\n            )\n        if fii_aligned:\n            reasons.append(\n                "FII net selling is accelerating, which often leads to sustained downward pressure on heavyweights."\n            )\n\n    if not reasons:\n        reasons = [\n            "Technical indicators are currently neutral. The market is in a wait-and-watch mode near key pivot levels."\n        ]\n\n    return {\n        "summary": f"Based on multi-source intelligence, I am seeing a {'BULLISH' if is_long else 'BEARISH'} bias with {sig_data['confidence']} confidence.",\n        "reasons": reasons,\n        "prediction": f"Targeting {entry_exit['target1']} with a strict stop-loss at {entry_exit['stop_loss']}.",\n        "risk_score": round(10 - (sig_data["score"] + 10) / 2, 1)\n        if is_long\n        else round((sig_data["score"] + 10) / 2, 1),\n    }\n\n\ndef _build_response(\n    symbol,\n    interval,\n    price,\n    chg,\n    sig_data,\n    agent_analysis,\n    qpct,\n    qlbl,\n    conditions,\n    met,\n    market_overview,\n    atm_iv,\n    vix_data,\n    fii_data,\n    fii_aligned,\n    opts_data,\n    news_data,\n    levels,\n    quant_conf,\n    quant_data,\n    mtf_data,\n    sr_zone,\n    adv_dec,\n    global_data,\n    monday,\n    entry_exit,\n    chart,\n    ns,\n    nr,\n    sup_str,\n    res_str,\n    elapsed,\n):\n    return {\n        "symbol": symbol,\n        "interval": interval,\n        "timestamp": datetime.now().strftime("%d/%m/%Y %H:%M:%S IST"),\n        "price": price,\n        "change_pct": chg,\n        "signal": sig_data,\n        "agent_analysis": agent_analysis,\n        "quality": {\n            "pct": qpct,\n            "label": qlbl,\n            "conditions": conditions,\n            "met": met,\n            "total": len(conditions),\n        },\n        "market_overview": market_overview,\n        "iv_rank": {\n            "iv_rank": None,\n            "iv_percentile": None,\n            "current_iv": atm_iv,\n            "regime": "HIGH IV" if (atm_iv or 0) > 20 else "NORMAL",\n            "source": "options_chain",\n        },\n        "vix": vix_data,\n        "fii_dii": {**fii_data, "aligned": fii_aligned},\n        "options": opts_data,\n        "news": news_data,\n        "news_sentiment": news_data,\n        "levels": levels,\n        "quant": {\n            "confidence": quant_conf,\n            "signal": quant_data.get("signal"),\n            "score": quant_data.get("score"),\n            "returns": quant_data.get("returns", {}),\n        },\n        "quant_mtf": {\n            "confidence": quant_conf,\n            "confidence_pct": round((quant_conf or 0) * 100, 1),\n            "signal": quant_data.get("signal"),\n            "score": quant_data.get("score"),\n            "returns": quant_data.get("returns", {}),\n            "mtf": mtf_data,\n        },\n        "sr_zone": sr_zone,\n        "signal_strength": {\n            "pct": min(100, max(0, round((sig_data["score"] + 10) / 20 * 100))),\n            "current_score": sig_data["score"],\n            "target_score": 9,\n            "gap_to_trade": max(0, 9 - sig_data["score"]),\n            "ready": sig_data["score"] >= 9,\n        },\n        "candle_momentum": market_overview.get("candle_momentum", {}),\n        "multitf": mtf_data,\n        "global": global_data,\n        "advance_decline": adv_dec,\n        "monday": monday,\n        "calendar": get_economic_calendar(),\n        "entry_exit": entry_exit,\n        "chart": chart,\n        "alert": (\n            f"Price within 0.5% of support {sup_str}"\n            if ns\n            else f"Price within 0.5% of resistance {res_str}"\n            if nr\n            else None\n        ),\n        "_perf": {"elapsed_seconds": elapsed},\n    }\n\n\ndef build_signaldesk(symbol, interval="1d"):\n    """build_signaldesk function.\n\n    Args:\n        symbol (Any): Parameter symbol.\n        interval (Any): Parameter interval.\n        \n    Returns:\n        Any: The computed result.\n        \n    Raises:\n        Exception: If the operation fails.\n        \n    Examples:\n        >>> result = build_signaldesk()\n    """\n    cache_key = f"signal_desk_v2:{symbol}:{interval}"\n    hit = C.get(cache_key)\n    if hit is not None:\n        return hit\n\n    t_start = _time.time()\n    from options_analyzer import analyze_options\n    from quant_mtf_engine import run_quant_mtf_engine\n\n    yf_sym = SYM_MAP.get(symbol.upper(), symbol)\n    if not yf_sym.startswith("^") and "." not in yf_sym:\n        yf_sym = yf_sym + ".NS"\n    tf_map = {\n        "5m": "5m",\n        "10m": "10m",\n        "15m": "15m",\n        "30m": "30m",\n        "1h": "60m",\n        "1d": "1d",\n    }\n    per_map = {\n        "5m": "5d",\n        "10m": "5d",\n        "15m": "5d",\n        "30m": "1mo",\n        "1h": "1mo",\n        "1d": "3mo",\n    }\n    tf = tf_map.get(interval, "1d")\n    period = per_map.get(interval, "3mo")\n\n    try:\n        df = yf.download(\n            yf_sym,\n            period=period,\n            interval=tf,\n            progress=False,\n            auto_adjust=True,\n            timeout=10,\n        )\n    except Exception:\n        df = pd.DataFrame()\n\n    if df.empty:\n        try:\n            ticker = yf.Ticker(yf_sym)\n            df = ticker.history(period=period, interval=tf, timeout=10)\n        except Exception as e:\n            raise ValueError(f"No data for {symbol}: {e}")\n\n    if df.empty:\n        raise ValueError(f"No data for {symbol}")\n\n    if not df.index.is_unique:\n        df = df.loc[~df.index.duplicated(keep="last")]\n\n    if isinstance(df.columns, pd.MultiIndex):\n        for i in range(df.columns.nlevels):\n            if "Close" in df.columns.get_level_values(i):\n                df.columns = df.columns.get_level_values(i)\n                break\n\n    closes = df["Close"]\n    price = safe(closes.iloc[-1])\n\n    ctx = _compute_market_context(df, price)\n    ts, chg, levels, atr_v, candle, rsi_v, mn, sn = (\n        ctx["ts"],\n        ctx["chg"],\n        ctx["levels"],\n        ctx["atr_v"],\n        ctx["candle"],\n        ctx["rsi_v"],\n        ctx["mn"],\n        ctx["sn"],\n    )\n    vol_confirms, stoch_k, stoch_d, sup_str, res_str = (\n        ctx["vol_confirms"],\n        ctx["stoch_k"],\n        ctx["stoch_d"],\n        ctx["sup_str"],\n        ctx["res_str"],\n    )\n    chart, market_overview, candle_momentum = (\n        ctx["chart"],\n        ctx["market_overview"],\n        ctx["candle_momentum"],\n    )\n\n    ext = {}\n    from data_fetcher import _EXECUTOR\n\n    futures = {\n        "vix": _EXECUTOR.submit(get_india_vix),\n        "global": _EXECUTOR.submit(get_global_markets),\n        "fii": _EXECUTOR.submit(get_fii_dii),\n        "mtf": _EXECUTOR.submit(get_multi_tf, yf_sym),\n        "news": _EXECUTOR.submit(get_news_sentiment, f"{symbol} Indian stock market"),\n        "adv_dec": _EXECUTOR.submit(get_advance_decline_cached, symbol),\n        "options": _EXECUTOR.submit(\n            analyze_options, symbol if symbol in ["NIFTY", "BANKNIFTY"] else "NIFTY"\n        ),\n        "quant": _EXECUTOR.submit(run_quant_mtf_engine, yf_sym),\n    }\n    for key, fut in futures.items():\n        try:\n            ext[key] = fut.result(timeout=10)\n        except Exception as e:\n            print(f"[SignalDesk] {key} failed: {e}")\n            ext[key] = _fallback(key)\n\n    (\n        vix_data,\n        global_data,\n        fii_data,\n        mtf_data,\n        news_data,\n        adv_dec,\n        opts_data,\n        quant_data,\n    ) = (\n        ext["vix"],\n        ext["global"],\n        ext["fii"],\n        ext["mtf"],\n        ext["news"],\n        ext["adv_dec"],\n        ext["options"],\n        ext.get("quant", {}),\n    )\n    quant_conf = quant_data.get("confidence", 0) or 0\n\n    # Execute Strategy Pattern\n    strategy_data = {\n        "opts_data": opts_data, "news_data": news_data, "ts": ts,\n        "mtf_data": mtf_data, "vix_data": vix_data, "global_data": global_data,\n        "fii_data": fii_data, "quant_conf": quant_conf, "quant_data": quant_data,\n        "price": price, "levels": levels, "atr_v": atr_v, "df": df\n    }\n    \n    strategy_results = {}\n    for contributor in _CONTRIBUTORS:\n        strategy_results.update(contributor.compute(strategy_data, strategy_results))\n        \n    opts_sentiment = strategy_results.get("options_sentiment", _compute_options_sentiment(opts_data))\n    oi_score, atm_iv = opts_sentiment["oi_score"], opts_sentiment["atm_iv"]\n    news_score = strategy_results.get("breadth_metrics", _compute_breadth_metrics(news_data))\n    \n    regime = strategy_results.get("regime")\n    if regime:\n        sig_data, is_long, fii_aligned = regime["sig_data"], regime["is_long"], regime["fii_aligned"]\n    else:\n        sig_data, is_long, fii_aligned = _compute_regime_signal(\n            ts, mtf_data, vix_data, global_data, fii_data, oi_score, news_score, quant_conf, quant_data\n        )\n\n    conditions = [\n        {\n            "label": "MACD confirms",\n            "met": (mn or 0) > (sn or 0) if is_long else (mn or 0) < (sn or 0),\n            "weight": 15,\n        },\n        {\n            "label": "MTF aligned",\n            "met": "BULL" in mtf_data.get("alignment", "")\n            if is_long\n            else "BEAR" in mtf_data.get("alignment", ""),\n            "weight": 15,\n        },\n        {"label": "OI not blocking", "met": abs(oi_score or 0) < 7, "weight": 15},\n        {"label": "Volume confirms", "met": vol_confirms, "weight": 15},\n        {"label": "FII aligned", "met": fii_aligned, "weight": 10},\n        {\n            "label": "RSI aligned",\n            "met": False\n            if rsi_v is None\n            else ((40 <= rsi_v <= 70) if is_long else (30 <= rsi_v <= 60)),\n            "weight": 5,\n        },\n        {"label": "Quant MTF confirms", "met": (quant_conf or 0) > 0.6, "weight": 5},\n        {\n            "label": "News supports",\n            "met": news_data.get("supports")\n            if is_long\n            else not news_data.get("supports"),\n            "weight": 5,\n        },\n        {\n            "label": "VIX acceptable",\n            "met": (vix_data.get("vix") or 99) < 20,\n            "weight": 5,\n        },\n        {\n            "label": "Global supports",\n            "met": global_data.get("supports")\n            if is_long\n            else not global_data.get("supports"),\n            "weight": 5,\n        },\n        {\n            "label": "StochRSI confirms",\n            "met": (stoch_k or 0) > (stoch_d or 0),\n            "weight": 5,\n        },\n        {\n            "label": "Candle confirms",\n            "met": ("BULL" in candle or candle == "HAMMER")\n            if is_long\n            else ("BEAR" in candle or candle == "SHOOTING_STAR"),\n            "weight": 5,\n        },\n    ]\n\n    total_weight = sum(c["weight"] for c in conditions)\n    earned_weight = sum(c["weight"] for c in conditions if c["met"])\n    qpct = round((earned_weight / total_weight) * 100, 1) if total_weight > 0 else 0\n    qlbl = (\n        "Strong Signal"\n        if qpct >= 90\n        else "Good Trade"\n        if qpct >= 75\n        else "Risky"\n        if qpct >= 60\n        else "Avoid"\n    )\n    met = sum(1 for c in conditions if c["met"])\n\n    tech = strategy_results.get("technical")\n    if tech:\n        entry_exit, sr_zone, ns, nr = tech["entry_exit"], tech["sr_zone"], tech["ns"], tech["nr"]\n    else:\n        entry_exit, sr_zone, ns, nr = _compute_trade_levels(price, levels, atr_v, sig_data.get("signal"))\n        \n    monday = strategy_results.get("volatility", _compute_risk_metrics(ts, vix_data, levels, price, atr_v))\n\n    agent_analysis = _compute_summary(\n        is_long,\n        ns,\n        nr,\n        sup_str,\n        res_str,\n        vix_data,\n        rsi_v,\n        mn,\n        sn,\n        vol_confirms,\n        global_data,\n        fii_aligned,\n        sig_data,\n        entry_exit,\n    )\n\n    elapsed = round(_time.time() - t_start, 2)\n    market_overview["candle_momentum"] = candle_momentum\n\n    result = _build_response(\n        symbol,\n        interval,\n        price,\n        chg,\n        sig_data,\n        agent_analysis,\n        qpct,\n        qlbl,\n        conditions,\n        met,\n        market_overview,\n        atm_iv,\n        vix_data,\n        fii_data,\n        fii_aligned,\n        opts_data,\n        news_data,\n        levels,\n        quant_conf,\n        quant_data,\n        mtf_data,\n        sr_zone,\n        adv_dec,\n        global_data,\n        monday,\n        entry_exit,\n        chart,\n        ns,\n        nr,\n        sup_str,\n        res_str,\n        elapsed,\n    )\n\n    C.put(cache_key, result, C.TTL_SIGNALDESK)\n    return result\n\n\ndef _fallback(key):\n    """Safe fallback values when a concurrent data fetch fails."""\n    defaults = {\n        "vix": {\n            "vix": None,\n            "change_pct": None,\n            "regime": "UNKNOWN",\n            "premium_multiplier": 1.0,\n            "strategy_bias": "NEUTRAL",\n            "history": [],\n        },\n        "global": {"markets": {}, "bullish_count": 0, "total": 0, "bias": "MIXED"},\n        "fii": {\n            "fii_net": 0,\n            "dii_net": 0,\n            "combined": 0,\n            "fii_bias": "NEUTRAL",\n            "dii_bias": "NEUTRAL",\n            "streak": 0,\n            "streak_note": "",\n            "note": "Data unavailable",\n            "is_weekend": False,\n            "history": [],\n        },\n        "mtf": {"timeframes": {}, "alignment": "MIXED"},\n        "news": {\n            "score": 0,\n            "label": "NEUTRAL",\n            "articles": [],\n            "total_articles": 0,\n            "avg_polarity": 0,\n            "note": "Unavailable",\n        },\n        "adv_dec": {\n            "advances": 0,\n            "declines": 0,\n            "unchanged": 0,\n            "total": 0,\n            "mood": "NEUTRAL",\n            "advance_pct": 0,\n            "note": "Unavailable",\n        },\n        "options": {\n            "symbol": "NIFTY",\n            "spot": None,\n            "error": "unavailable",\n            "pcr": {"pcr": 1.0, "signal": "neutral"},\n            "max_pain": None,\n            "oi_score": 0,\n            "atm_iv": None,\n            "top_ce_oi": [],\n            "top_pe_oi": [],\n        },\n        "quant": {"confidence": 0, "signal": "NEUTRAL", "score": 0, "returns": {}},\n    }\n    return defaults.get(key, {})
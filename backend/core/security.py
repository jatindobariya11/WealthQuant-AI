import os\nimport time\nfrom typing import List, Optional\n\nimport jwt\nfrom fastapi import Depends, HTTPException, WebSocket, status\nfrom fastapi.security import HTTPAuthorizationCredentials, HTTPBearer\n\nsecurity = HTTPBearer()\n\n# Load configuration from environment\n# FIX-003: Secret Management (No fallback secrets)\nSECRET_KEY = os.getenv("JWT_SECRET_KEY")\nif not SECRET_KEY:\n    raise RuntimeError(\n        "CRITICAL ERROR: JWT_SECRET_KEY environment variable is missing. Halting startup."\n    )\n\nALGORITHM = "HS256"\nACCESS_TOKEN_EXPIRE_MINUTES = 15\nREFRESH_TOKEN_EXPIRE_DAYS = 7\n\n\ndef create_access_token(\n    """create_access_token function.\n\n    Args:\n        data (Any): Parameter data.\n        expires_delta (Any): Parameter expires_delta.\n        \n    Returns:\n        Any: The computed result.\n        \n    Raises:\n        Exception: If the operation fails.\n        \n    Examples:\n        >>> result = create_access_token()\n    """\n    data: dict, expires_delta: int = ACCESS_TOKEN_EXPIRE_MINUTES * 60\n):\n    to_encode = data.copy()\n    expire = time.time() + expires_delta\n    to_encode.update({"exp": expire})\n    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)\n\n\ndef create_refresh_token(\n    """create_refresh_token function.\n\n    Args:\n        data (Any): Parameter data.\n        expires_delta (Any): Parameter expires_delta.\n        \n    Returns:\n        Any: The computed result.\n        \n    Raises:\n        Exception: If the operation fails.\n        \n    Examples:\n        >>> result = create_refresh_token()\n    """\n    data: dict, expires_delta: int = REFRESH_TOKEN_EXPIRE_DAYS * 86400\n):\n    to_encode = data.copy()\n    expire = time.time() + expires_delta\n    to_encode.update({"exp": expire, "type": "refresh"})\n    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)\n\n\ndef decode_token(token: str):\n    """decode_token function.\n\n    Args:\n        token (Any): Parameter token.\n        \n    Returns:\n        Any: The computed result.\n        \n    Raises:\n        Exception: If the operation fails.\n        \n    Examples:\n        >>> result = decode_token()\n    """\n    try:\n        return jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])\n    except jwt.ExpiredSignatureError:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail="Signature expired. Please re-authenticate.",\n        )\n    except jwt.PyJWTError as e:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=f"Authentication failed: {str(e)}",\n        )\n\n\ndef verify_token(credentials: HTTPAuthorizationCredentials = Depends(security)) -> dict:\n    """\n    HTTPBearer authentication token dependency validation.\n    Returns the decoded token payload.\n    """\n    token = credentials.credentials\n    payload = decode_token(token)\n    user_id = payload.get("sub")\n    if not user_id:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail="Invalid credentials: Subject claim not found",\n        )\n    return payload\n\n\nasync def verify_token_websocket(websocket: WebSocket) -> Optional[dict]:\n    """\n    WebSocket Authentication (FIX-001)\n    Extracts token from query parameters and validates it.\n    """\n    token = websocket.query_params.get("token")\n    if not token:\n        await websocket.close(code=status.WS_1008_POLICY_VIOLATION)\n        return None\n    try:\n        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])\n        return payload\n    except jwt.PyJWTError:\n        await websocket.close(code=status.WS_1008_POLICY_VIOLATION)\n        return None\n\n\n# FIX-002: Role-Based Access Control (RBAC)\nclass RoleChecker:\n    def __init__(self, allowed_roles: List[str]):\n        self.allowed_roles = allowed_roles\n\n    def __call__(self, payload: dict = Depends(verify_token)):\n        user_roles = payload.get("roles", [])\n        if not any(role in self.allowed_roles for role in user_roles):\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail="Operation not permitted: Insufficient privileges",\n            )\n        return payload
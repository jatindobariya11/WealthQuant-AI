import os\nfrom datetime import datetime\n\nimport pandas as pd\nimport requests\n\nimport cache as C\nimport yfinance as yf\nfrom base_indicators import safe\nfrom core.shared_features import *\n\nfrom .config import *\n\n\ndef get_india_vix():\n    """get_india_vix function.\n\n    Args:\n        None\n        \n    Returns:\n        Any: The computed result.\n        \n    Raises:\n        Exception: If the operation fails.\n        \n    Examples:\n        >>> result = get_india_vix()\n    """\n    hit = C.get("vix")\n    if hit is not None:\n        return hit\n    try:\n        t = yf.Ticker("^INDIAVIX")\n        i = t.fast_info\n        vix = safe(i.last_price)\n        chg = (\n            safe(\n                (float(i.last_price) - float(i.previous_close))\n                / float(i.previous_close)\n                * 100\n            )\n            if i.previous_close\n            else 0\n        )\n        vix_hist = []\n        try:\n            vdf = yf.download(\n                "^INDIAVIX",\n                period="1mo",\n                interval="1d",\n                progress=False,\n                auto_adjust=True,\n                timeout=10,\n            )\n            if not vdf.empty:\n                vdf = vdf.loc[~vdf.index.duplicated(keep="last")]\n\n                # FIX-002 Validation\n                validation_errors = []\n                if vdf.index.tzinfo is None:\n                    validation_errors.append("Timezone mismatch")\n\n                diffs = vdf.index.to_series().diff().dropna()\n                if (diffs > pd.Timedelta("3D")).any():  # Allow for weekends\n                    validation_errors.append("Missing daily candles")\n\n                if validation_errors:\n                    print(f"[VIX] Validation degraded: {validation_errors}")\n\n                # Robust column flattening — find the level that contains 'Close'\n                if isinstance(vdf.columns, pd.MultiIndex):\n                    for lvl in range(vdf.columns.nlevels):\n                        if "Close" in vdf.columns.get_level_values(lvl):\n                            vdf.columns = vdf.columns.get_level_values(lvl)\n                            break\n                    else:\n                        vdf.columns = vdf.columns.get_level_values(-1)\n\n                vix_hist = []\n                if "Close" in vdf.columns:\n                    for d, v in zip(vdf.index, vdf["Close"]):\n                        val = safe(v)\n                        if val is not None:\n                            vix_hist.append({"date": str(d)[:10], "vix": val})\n        except Exception as e:\n            print(f"[VIX] History download failed: {e}")\n            pass\n        result = {\n            "vix": vix,\n            "change_pct": chg,\n            "regime": "HIGH FEAR"\n            if (vix or 0) > 20\n            else "MODERATE"\n            if (vix or 0) > 14\n            else "LOW",\n            "premium_multiplier": safe(1 + (vix - 15) * 0.02)\n            if (vix and vix > 15)\n            else 1.0,\n            "strategy_bias": "FAVOR SELLING"\n            if (vix or 0) > 18\n            else "NEUTRAL"\n            if (vix or 0) > 12\n            else "BUY OPTIONS",\n            "history": vix_hist[-30:],\n        }\n        C.put("vix", result, C.TTL_VIX)\n        return result\n    except Exception as e:\n        print(f"[VIX] Fetch failed: {e}")\n        return {\n            "vix": None,\n            "change_pct": None,\n            "regime": "UNKNOWN",\n            "premium_multiplier": 1.0,\n            "strategy_bias": "NEUTRAL",\n            "history": [],\n        }\n\n\ndef get_global_markets():\n    """get_global_markets function.\n\n    Args:\n        None\n        \n    Returns:\n        Any: The computed result.\n        \n    Raises:\n        Exception: If the operation fails.\n        \n    Examples:\n        >>> result = get_global_markets()\n    """\n    hit = C.get("global_markets")\n    if hit is not None:\n        return hit\n    result = {}\n    bull = 0\n    try:\n        # Single batch download — 1 HTTP call. Increased period to 5d for robustness.\n        tickers_str = " ".join(_GLOBAL_SYMS.values())\n        df = yf.download(\n            tickers_str,\n            period="5d",\n            interval="1d",\n            progress=False,\n            auto_adjust=True,\n            group_by="ticker",\n            timeout=20,\n        )\n\n        if not df.empty:\n            df = df.loc[~df.index.duplicated(keep="last")]\n\n        # Collect what we already have from batch df\n        batch_results = {}\n        missing_syms = {}\n        for name, sym in _GLOBAL_SYMS.items():\n            sub = pd.DataFrame()\n            if not df.empty:\n                if isinstance(df.columns, pd.MultiIndex):\n                    if sym in df.columns.get_level_values(0):\n                        sub = df[sym].dropna(subset=["Close"])\n                elif sym in df.columns:\n                    sub = df[[sym]].dropna()\n\n            if not sub.empty and len(sub) >= 2:\n                batch_results[name] = sub\n            else:\n                missing_syms[name] = sym\n\n        # Fetch missing symbols in parallel\n        if missing_syms:\n            from concurrent.futures import ThreadPoolExecutor, as_completed\n\n            def _fetch_single(n, s):\n                try:\n                    single_df = yf.download(\n                        s,\n                        period="5d",\n                        interval="1d",\n                        progress=False,\n                        auto_adjust=True,\n                        timeout=5,\n                    )\n                    if not single_df.empty:\n                        single_df = single_df.loc[\n                            ~single_df.index.duplicated(keep="last")\n                        ]\n                        if isinstance(single_df.columns, pd.MultiIndex):\n                            for i in range(single_df.columns.nlevels):\n                                if "Close" in single_df.columns.get_level_values(i):\n                                    single_df.columns = (\n                                        single_df.columns.get_level_values(i)\n                                    )\n                                    break\n                        single_df = single_df.dropna(subset=["Close"])\n                        return n, single_df\n                except:\n                    pass\n                return n, pd.DataFrame()\n\n            with ThreadPoolExecutor(max_workers=min(len(missing_syms), 8)) as executor:\n                futures = {\n                    executor.submit(_fetch_single, n, s): n\n                    for n, s in missing_syms.items()\n                }\n                for fut in as_completed(futures, timeout=6):\n                    try:\n                        n, single_df = fut.result()\n                        if not single_df.empty and len(single_df) >= 2:\n                            batch_results[n] = single_df\n                    except:\n                        pass\n\n        # Process results\n        for name, sym in _GLOBAL_SYMS.items():\n            try:\n                sub = batch_results.get(name)\n                if sub is None or sub.empty or len(sub) < 2:\n                    result[name] = {"price": None, "chg_pct": None}\n                    continue\n\n                # Ensure sub columns are flat\n                if isinstance(sub.columns, pd.MultiIndex):\n                    sub.columns = sub.columns.get_level_values(-1)\n\n                last = safe(sub["Close"].iloc[-1])\n                prev = safe(sub["Close"].iloc[-2])\n                chg = safe((last - prev) / prev * 100) if (last and prev) else 0\n                result[name] = {"price": last, "chg_pct": chg}\n                if chg and chg > 0:\n                    bull += 1\n            except Exception as e:\n                print(f"[GlobalMarkets] Failed to process {name} ({sym}): {e}")\n                result[name] = {"price": None, "chg_pct": None}\n\n    except Exception as e:\n        print(f"[GlobalMarkets] Batch fetch failed: {e}")\n        for name in _GLOBAL_SYMS:\n            result[name] = {"price": None, "chg_pct": None}\n\n    sp_chg = result.get("S&P 500", {}).get("chg_pct")\n    nq_chg = result.get("Nasdaq 100", {}).get("chg_pct")\n    global_supports = bool(\n        sp_chg is not None and sp_chg > 0 and nq_chg is not None and nq_chg > 0\n    )\n\n    total_keys = len(_GLOBAL_SYMS)\n    out = {\n        "markets": result,\n        "bullish_count": bull,\n        "total": total_keys,\n        "bias": "BULLISH"\n        if bull >= round(total_keys * 0.55)\n        else "BEARISH"\n        if bull <= round(total_keys * 0.3)\n        else "MIXED",\n        "supports": global_supports,\n        "sp500": result.get("S&P 500", {"price": None, "chg_pct": None}),\n        "nasdaq": result.get("Nasdaq 100", {"price": None, "chg_pct": None}),\n        "dxy": result.get("Dollar Index", {"price": None, "chg_pct": None}),\n    }\n    C.put("global_markets", out, C.TTL_GLOBAL)\n    return out\n\n\ndef get_news_sentiment(query="Nifty Indian stock market", max_articles=10):\n    """FIX #7,#15,#20 — consolidated here with safe TextBlob import."""\n    cache_key = f"news:{query[:40]}"\n    hit = C.get(cache_key)\n    if hit is not None:\n        return hit\n\n    try:\n        from textblob import TextBlob  # type: ignore\n\n        has_tb = True\n    except:\n        has_tb = False\n\n    articles = []\n    # Attempt to get API key from environment\n    api_key = os.getenv("NEWS_API_KEY")\n    if api_key:\n        try:\n            resp = requests.get(\n                "https://newsapi.org/v2/everything",\n                params={\n                    "q": query,\n                    "language": "en",\n                    "sortBy": "publishedAt",\n                    "pageSize": max_articles,\n                    "apiKey": api_key,\n                },\n                timeout=12,\n            )\n            articles = [\n                {\n                    "title": a["title"],\n                    "url": a.get("url", ""),\n                    "source": a["source"]["name"],\n                    "published": a.get("publishedAt", "")[:10],\n                }\n                for a in resp.json().get("articles", [])\n                if a.get("title")\n            ]\n        except:\n            pass\n    if not articles:\n        try:\n            t = yf.Ticker("^NSEI")\n            news = t.news or []\n            articles = [\n                {\n                    "title": n.get("title", ""),\n                    "url": n.get("link", ""),\n                    "source": n.get("publisher", "Yahoo"),\n                    "published": datetime.fromtimestamp(\n                        n.get("providerPublishTime", 0)\n                    ).strftime("%Y-%m-%d"),\n                }\n                for n in news[:max_articles]\n                if n.get("title") and n.get("providerPublishTime", 0) > 0\n            ]\n        except:\n            pass\n    if not articles:\n        result = {\n            "score": 0,\n            "label": "NEUTRAL",\n            "articles": [],\n            "total_articles": 0,\n            "avg_polarity": 0,\n            "note": "No news available",\n        }\n        C.put(cache_key, result, C.TTL_NEWS)\n        return result\n\n    POSITIVE = [\n        "surge",\n        "rally",\n        "gains",\n        "bullish",\n        "record",\n        "growth",\n        "profit",\n        "beats",\n        "strong",\n        "up",\n    ]\n    NEGATIVE = [\n        "crash",\n        "fall",\n        "drop",\n        "bearish",\n        "loss",\n        "weak",\n        "down",\n        "cut",\n        "miss",\n        "risk",\n    ]\n\n    scored = []\n    total_pos = 0\n    total_neg = 0\n    for a in articles:\n        if not a.get("title"):\n            continue\n        title_lower = a["title"].lower()\n        pos = sum(1 for w in POSITIVE if w in title_lower)\n        neg = sum(1 for w in NEGATIVE if w in title_lower)\n\n        try:\n            pol = (\n                (TextBlob(a["title"]).sentiment.polarity)\n                if has_tb\n                else (0.3 if pos > neg else -0.3 if neg > pos else 0)\n            )\n            lbl = "BULLISH" if pol > 0.1 else "BEARISH" if pol < -0.1 else "NEUTRAL"\n            scored.append({**a, "sentiment": lbl, "polarity": round(pol, 3)})\n            total_pos += pos\n            total_neg += neg\n        except:\n            scored.append({**a, "sentiment": "NEUTRAL", "polarity": 0})\n\n    score = total_pos - total_neg\n    result = {\n        "score": score,\n        "positive": total_pos,\n        "negative": total_neg,\n        "supports": score > 0,\n        "label": "BULLISH" if score > 0 else "BEARISH" if score < 0 else "NEUTRAL",\n        "articles": scored[:8],\n        "total_articles": len(scored),\n        "note": "" if has_tb else "Keyword fallback active",\n    }\n    C.put(cache_key, result, C.TTL_NEWS)\n    return result
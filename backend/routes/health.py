from fastapi import APIRouter, Depends, HTTPException, BackgroundTasks, WebSocket, WebSocketDisconnect, Request\nfrom fastapi.responses import JSONResponse\nimport asyncio\nimport time\nimport json\nimport logging\nfrom typing import Optional\n\n# import whatever is needed\nfrom core.shared_features import *\nimport cache\nimport database as DB\nfrom prediction_engine import *\n\nrouter = APIRouter()\n\n@router.get("/api/metrics")\nasync def get_platform_metrics():\n    """\n    Real-time platform performance metrics.\n    Returns API latency, cache hit ratio, prediction store stats,\n    dashboard performance, and scheduler status.\n    """\n    with _api_latencies_lock:\n        lats = list(_api_latencies)\n\n    if lats:\n        sorted_lats = sorted(lats)\n        n = len(sorted_lats)\n        p50 = sorted_lats[int(n * 0.50)]\n        p95 = sorted_lats[min(int(n * 0.95), n - 1)]\n        p99 = sorted_lats[min(int(n * 0.99), n - 1)]\n    else:\n        p50 = p95 = p99 = 0.0\n\n    cache_stats = cache.status()\n    pred_stats = prediction_store.stats()\n    dash_perf = get_dashboard_perf()\n\n    try:\n        sched_status = wq_scheduler.status()\n        sched_running = sched_status.get("is_running", False)\n        sched_last_run_ms = sched_status.get("last_ingestion_duration_ms", 0)\n    except (RuntimeError, AttributeError):\n        sched_running = False\n        sched_last_run_ms = 0\n\n    db_connected = pipeline_db.is_connected\n\n    return {\n        "timestamp": datetime.utcnow().isoformat() + "Z",\n        "api_latency": {\n            "p50_ms": round(p50, 1),\n            "p95_ms": round(p95, 1),\n            "p99_ms": round(p99, 1),\n            "samples": len(lats),\n        },\n        "dashboard": dash_perf,\n        "cache": {\n            "hit_ratio": cache_stats.get("hit_rate", 0),\n            "total_keys": cache_stats.get("total_keys", 0),\n            "alive_keys": cache_stats.get("alive_keys", 0),\n        },\n        "prediction_store": pred_stats,\n        "database": {\n            "connected": db_connected,\n            "status": "LIVE" if db_connected else "OFFLINE",\n        },\n        "scheduler": {\n            "running": sched_running,\n            "last_run_ms": sched_last_run_ms,\n        },\n    }\n\n@router.get("/health")\ndef health_check():\n    """health_check function.\n\n    Args:\n        None\n        \n    Returns:\n        Any: The computed result.\n        \n    Raises:\n        Exception: If the operation fails.\n        \n    Examples:\n        >>> result = health_check()\n    """\n    return {\n        "status": "ok",\n        "uptime": "running",\n        "database": "live" if pipeline_db.is_connected else "degraded",\n        "scheduler": "running" if wq_scheduler.is_running() else "degraded",\n    }\n\n@router.get("/health/full")\ndef health_check_full():\n    """health_check_full function.\n\n    Args:\n        None\n        \n    Returns:\n        Any: The computed result.\n        \n    Raises:\n        Exception: If the operation fails.\n        \n    Examples:\n        >>> result = health_check_full()\n    """\n    import psutil\n\n    from core.thread_pools import (\n        db_pool,\n        nse_pool,\n        playwright_pool,\n        research_pool,\n        yahoo_pool,\n    )\n\n    vm = psutil.virtual_memory()\n    return {\n        "status": "ok",\n        "uptime": "running",\n        "memory": {\n            "heap_percent": round(vm.percent, 1),\n            "available_gb": round(vm.available / (1024**3), 2),\n            "used_gb": round(vm.used / (1024**3), 2),\n            "total_gb": round(vm.total / (1024**3), 2),\n        },\n        "subsystems": {\n            "API": "Healthy",\n            "PostgreSQL": "Healthy"\n            if pipeline_db.is_connected\n            else "Degraded (Offline)",\n            "Prediction Engine": "Healthy",\n            "Scheduler": "Healthy" if wq_scheduler.is_running() else "Degraded",\n            "Cache": "Healthy",\n            "NSE": "Healthy",\n            "Yahoo": "Healthy",\n            "Playwright": "Healthy",\n        },\n        "thread_pools": {\n            "playwright_pool_active": playwright_pool._work_queue.qsize(),\n            "nse_pool_active": nse_pool._work_queue.qsize(),\n            "yahoo_pool_active": yahoo_pool._work_queue.qsize(),\n            "db_pool_active": db_pool._work_queue.qsize(),\n            "research_pool_active": research_pool._work_queue.qsize(),\n        },\n    }\n